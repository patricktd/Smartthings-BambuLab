# capabilities/custom.bambuPrinterStatus.yaml
id: custom.bambuPrinterStatus
version: 1
status: draft
name: Bambu Printer Status
attributes:
  printerStatus:
    schema:
      type: string
      enum: [printing, pause, stop]
    required: true
commands: {}
